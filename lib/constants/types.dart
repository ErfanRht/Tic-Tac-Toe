enum PlayMode { AI, Friend }
enum UserSide { CROSS, ZERO }
enum IconSize { MINIMAL, MEDIUM, MAXIMAL }
enum UserMode { USER, FRIEND }
enum WinnerMode { USER, FRIEND, TIE, NONE }
enum LineMode { VERTICAL, HORIZONTAL }
enum BoxValue { EMPTY, CROSS, ZERO }
enum Turn { CROSS, ZERO }
enum IntoBox { TABLE, RESULT }
